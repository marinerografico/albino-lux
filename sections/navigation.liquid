<nav class="fixed flex mix-blend-difference w-full z-40 pt-8 pb-8 top-0 left-0 justify-between items-center px-6">
  <a href="{{ routes.root_url }}" class="hover:opacity-80 transition-opacity lowercase leading-none text-3xl font-bold text-neutral-900 tracking-tighter">
    albino.
  </a>
  <button onclick="toggleCart()" class="cart-link text-xs text-neutral-900 hover:opacity-70 transition-opacity lowercase tracking-wide font-medium">
    <span class="cart-text">ritual</span>
    <span class="cart-count hidden">(<span id="cart-item-count">0</span>)</span>
  </button>
</nav>

{% schema %}
{
  "name": "Navigation",
  "settings": []
}
{% endschema %}

